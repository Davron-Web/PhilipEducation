<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Платёжный провайдер
    |--------------------------------------------------------------------------
    |
    | sandbox — тестовый режим: подтверждает оплату без реальных денег,
    | нужен для разработки. На боевом сервере (APP_ENV=production) он
    | принудительно запрещён, см. PaymentServiceProvider.
    |
    | Реальный провайдер (Алиф, DC Pay и т.п.) добавляется сюда новым
    | классом, реализующим App\Services\Payment\PaymentGateway.
    |
    */
    'gateway' => env('PAYMENT_GATEWAY', 'sandbox'),

    'gateways' => [
        'sandbox' => App\Services\Payment\SandboxGateway::class,
    ],

    /*
    |--------------------------------------------------------------------------
    | Бесплатный доступ
    |--------------------------------------------------------------------------
    |
    | Уровни, материалы которых открыты без подписки. Всё, что выше,
    | требует активной подписки.
    |
    */
    'free_levels' => ['A1', 'A2'],

    /*
    |--------------------------------------------------------------------------
    | Разделы по подписке
    |--------------------------------------------------------------------------
    |
    | Phil здесь потому, что каждый его ответ стоит денег (запрос к Gemini),
    | а не только из соображений монетизации.
    |
    */
    'paid_sections' => ['ielts', 'books', 'phil'],
];
