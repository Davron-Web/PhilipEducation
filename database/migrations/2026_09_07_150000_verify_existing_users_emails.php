<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    /**
     * Считает почту уже зарегистрированных пользователей подтверждённой.
     *
     * Со следующей правкой доступ к урокам, тестам и словарю закрывается до
     * подтверждения email. Все существующие аккаунты созданы, когда такого
     * требования не было, и без этой миграции 1108 человек одновременно
     * оказались бы заперты на странице «подтвердите почту» — с письмом,
     * которое многие уже не получат (адреса вроде @example.com у тестовых
     * учётных записей просто не существуют).
     *
     * Дату ставим не now(), а created_at: «подтверждено тогда же, когда
     * зарегистрирован» ближе к правде, чем «подтверждено в день миграции»,
     * и не искажает возможную аналитику по срокам подтверждения.
     */
    public function up(): void
    {
        DB::table('users')
            ->whereNull('email_verified_at')
            ->update(['email_verified_at' => DB::raw('created_at')]);
    }

    /**
     * Откат не снимает подтверждение: отличить тех, кому его проставила эта
     * миграция, от подтвердивших почту по-настоящему уже нельзя, а массовый
     * сброс запер бы живых пользователей.
     */
    public function down(): void
    {
        //
    }
};
